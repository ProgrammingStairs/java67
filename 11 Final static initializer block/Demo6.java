Static keyword:
---------------
1. variable
    If you make any variable as a static then in that case that variable is considered as a class variable.

    Static variable is directly accessible by class name, we do not needs class object to access that specific static variable. 

    static variables (class variables) have only one copy.

2. method : 
    If you make any method as a static then in that case that method is considered as a class method.

    Static method is directly accessible by class name, we do not needs class object to call that specific static method. 

3. block: 
    static block executes before main method.
    static block executes when class loads.

4. class:
    we can also create static nested class